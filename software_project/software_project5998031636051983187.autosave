import g4p_controls.*;

Questions geoEasy, geoMedium, geoHard;

int lvl;

String answer, question;

void setup() {
  size(800,800);
  background(255);
  
  // Loads the relevant text file, depending on the slider value
  loadQuestions();
  createGUI();
  
  answer = geoEasy.getAnswer();
  question = geoEasy.getQuestion();
  
  println(question);
  println(answer);
}

void draw() {
  textSize(50);
  text(question, 40, 120);
  fill(0);
}
